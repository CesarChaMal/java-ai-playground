ollama pull mistral
ollama run mistral

ollama pull nomic-embed-text
ollama run nomic-embed-text

./mvnw clean install


//Compile Frontend Separately
./mvnw vaadin:build-frontend -X
./mvnw vaadin:prepare-frontend vaadin:build-frontend


curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"


curl https://api.openai.com/v1/embeddings \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "text-embedding-3-large",
    "input": "hello world"
  }'


# if problems

rm -rf node_modules
rm -rf frontend/generated
rm -rf target
rm package-lock.json

./mvnw clean vaadin:prepare-frontend vaadin:build-frontend
./mvnw install
./mvnw spring-boot:run -Dspring-boot.run.arguments="--debug"

# default openai
./mvnw spring-boot:run
./mvnw spring-boot:run -Dspring-boot.run.profiles=openai
./mvnw spring-boot:run -Dspring-boot.run.profiles=ollama

./mvnw clean install && ./mvnw spring-boot:run -Dspring-boot.run.profiles=openai
./mvnw clean install && ./mvnw spring-boot:run -Dspring-boot.run.profiles=ollama


$ curl http://localhost:11434/api/tags

# chat
curl -s http://localhost:11434/api/generate \
  -d '{"model":"mistral","prompt":"Say hello"}'

# embeddings
curl -s http://localhost:11434/api/embeddings \
  -d '{"model":"nomic-embed-text","prompt":"hello world"}'

# Ollama tooling is not supported for now
https://spring.io/blog/2024/07/26/spring-ai-with-ollama-tool-support
